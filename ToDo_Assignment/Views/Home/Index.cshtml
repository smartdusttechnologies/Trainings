@model IEnumerable<ToDo_Assignment.Models.ToDoModel>

@{
    ViewData["Title"] = "Home Page";
}

<script>
    function fun(id, status) {
        if (status == "Completed") {
            if (confirm("Do you want to update completed task to new???") == true)
            {
                callBackEnd(id, status);
            }
        }
        else
        {

            callBackEnd(id, status);
        }

        function callBackEnd(id, status)
        {
            var form = document.createElement("form");
            form.setAttribute("method", "post");
            form.setAttribute("action", "Home/updateData");

            var FN = document.createElement("input");
            FN.setAttribute("type", "text");
            FN.setAttribute("name", "id");
            FN.setAttribute("value", id.id);

            form.appendChild(FN);

            var FNN = document.createElement("input");
            FNN.setAttribute("type", "text");
            FNN.setAttribute("name", "status");
            FNN.setAttribute("value", status);

            form.appendChild(FNN);

            document.body.appendChild(form);
            form.submit();
        }
    }
</script>

<div class="container-fluid">
    <div class="row">
        <div class="col-sm-4 container ">
            <table class="table table-borderless bg-white " style="background-color: ghostwhite;">
                @{
                    int new_count = 0;
                }
                @foreach (var c in Model.Where(n => n.TStatus == "new"))
                {
                    new_count = new_count + 1;
                }
                <tr class="border-bottom  ">
                    <th>
                        To Do(@new_count)
                        <br>
                        <progress id="progress" value="@new_count" max="10" style="width: 100%;"></progress>
                    </th>
                </tr>
                <tr class="border-bottom">
                    <td> <a style="color: #10c3dc" data-toggle="modal" data-target="#myModal"><i class='fas fa-plus'></i>&nbsp;&nbsp;&nbsp;Add task</a></td>
                </tr>
                @foreach (var task in Model.Where(n => n.TStatus == "new"))
                {
                    <tr class="border-bottom">
                        @{
                            var bg_color = "";
                            DateTime TodayDate = DateTime.Today;
                            if (task.DueDate < TodayDate)
                            {
                                bg_color = "bg-warning";
                            }
                            var shortDateValue = @task.DueDate.ToShortDateString();
                        }
                        <td class="@bg_color">
                            <input id="@task.ID" class="input" type="checkbox" onclick="fun(this,'new')" name="task" value="@task.Task" />&nbsp;<span><a href="" onclick="setData('@task.ID', '@task.Task', '@task.Description', '@task.AssignedTo','@task.StoryPoints', '@task.DueDate')" data-toggle="modal" data-target="#myModalview">@task.Task</a>&nbsp;</span>
                            <span class="float-right"> @shortDateValue</span>
                            <p style="color:forestgreen">
                                Assignee: @{
                                    if (task.AssignedTo == null)
                                    {
                                        task.AssignedTo = "Unassigned";
                                    } @task.AssignedTo
                                }
                            </p><a href="/Home/DeleteEToDo/@task.ID" onclick="return confirm('Are you sure?');" class="btn btn-sm btn-danger">Delete</a>
                            <span class="float-right" style="border-style:solid; border-color:green; border-width: thin;  border-spacing: 15px 50px;">&nbsp;@task.StoryPoints &nbsp;</span>
                        </td>
                    </tr>
                }
            </table>
        </div>

        <div class="col-sm-4 ">
            <table class="table table-borderless bg-white " style="background-color: ghostwhite;">
                <tr class="border-bottom">
                    @{
                        int progress_count = 0;
                    }
                    @foreach (var c in Model.Where(n => n.TStatus == "InProgress"))
                    {
                        progress_count = progress_count + 1;
                    }
                    <th>
                        In Progress(@progress_count)
                        <br>
                        <progress id="progress" value="@progress_count" max="10" style="width: 100%;"></progress>
                    </th>
                </tr>
                <tr class="border-bottom">
                    <th><p></p></th>
                </tr>
                @foreach (var task in Model.Where(n => n.TStatus == "InProgress"))
                {
                    <tr class="border-bottom">
                        @{
                            var bg_color = "";
                            DateTime TodayDate = DateTime.Today;
                            if (task.DueDate < TodayDate)
                            {
                                bg_color = "bg-warning";
                            }
                            var shortDateValue = @task.DueDate.ToShortDateString();
                        }
                        <td class="@bg_color">
                            <input id="@task.ID" class="input" type="checkbox" name="task" onclick="fun(this,'InProgress')" value="@task.Task" />&nbsp;<span><a href="" onclick="setData('@task.ID', '@task.Task', '@task.Description', '@task.AssignedTo', '@task.StoryPoints', '@task.DueDate')" data-toggle="modal" data-target="#myModalview">@task.Task</a>&nbsp;</span><span class="float-right"> @shortDateValue</span>
                            <p style="color:forestgreen">
                                Assignee:  @{
                                    if (task.AssignedTo == null) { task.AssignedTo = "Unassigned"; }
                                    @task.AssignedTo;
                                }
                            </p>
                            <a href="/Home/DeleteEToDo/@task.ID" onclick="return confirm('Are you sure?');" class="btn btn-sm btn-danger">Delete</a>
                            <span class="float-right" style="border-style:solid; border-color:green; border-width: thin;  border-spacing: 15px 50px;">&nbsp;@task.StoryPoints &nbsp;</span>
                        </td>
                    </tr>
                }
            </table>
        </div>
        <div class="col-sm-4">
            <table class="table table-borderless bg-white " style="background-color: ghostwhite;">
                <tr class="border-bottom">
                    @{
                        int completed_count = 0;
                    }
                    @foreach (var c in Model.Where(n => n.TStatus == "Completed"))
                    {
                        completed_count = completed_count + 1;
                    }
                    <th>
                        Completed(@completed_count)
                        <br>
                        <progress id="progress" value="@completed_count" max="10" style="width: 100%;"></progress>
                    </th>
                </tr>
                <tr class="border-bottom">
                    <th><p></p></th>
                </tr>
                @foreach (var task in Model.Where(n => n.TStatus == "Completed"))
                {
                    <tr class="border-bottom">
                        @{
                            var shortDateValue = @task.DueDate.ToShortDateString();
                        }
                        <td>
                            <div>
                                <input id="@task.ID" class="form-check-inline" type="checkbox" name="task" onclick="fun(this,'Completed')" value="@task.Task" checked />&nbsp;<span><a href="" onclick="setData('@task.ID', '@task.Task', '@task.Description', '@task.AssignedTo', '@task.StoryPoints', '@task.DueDate')" data-toggle="modal" data-target="#myModalview">@task.Task</a>&nbsp;</span><span class="float-right"> @shortDateValue</span>
                                <p style="color:forestgreen">
                                    Assignee: @{
                                        if (task.AssignedTo == null) { task.AssignedTo = "Unassigned"; }
                                        @task.AssignedTo;
                                    }
                                </p>
                            </div>
                            <a href="/Home/DeleteEToDo/@task.ID" onclick="return confirm('Are you sure?');" class="btn btn-sm btn-danger">Delete</a>
                            <span class="float-right" style="border-style:solid; border-color:green; border-width: thin;  border-spacing: 15px 50px;">&nbsp;@task.StoryPoints &nbsp;</span>
                        </td>
                    </tr>
                }
            </table>
        </div>
    </div>
</div>

@*Grid view code vill be there on GridView.cshtml*@
@Html.Partial("~/Views/Home/GridView.cshtml")
@Html.Partial("~/Views/Home/MicroGridView.cshtml")

<!-- Modal -->
<div class="modal fade" id="myModal" role="dialog">
    <div class="modal-dialog">

        <!-- Modal content-->
        <div class="modal-content">

            <div class="modal-header" style="background: #2cbac4;">
                <h4 class="modal-title">Add Task</h4>
                <button type="button" class="close" data-dismiss="modal">&times;</button>
            </div>

            <div class="modal-body">

                <form class="form" method="post" action="Home/InsertEToDo">
                    <div class="form-group">
                        <label for="note">Task</label>
                        <input type="text" class="form-control" id="note" placeholder="Name" required name="Task">
                    </div>
                    <div class="form-group">
                        <lable form="note">Description</lable>
                        <textarea rows="5" cols="60" class="form-control" id="Description" placeholder="Enter Work Description Here" required name="Description"> </textarea>
                    </div>
                    <div class="form-group">
                        <label for="note">Assigned To</label>
                        <input type="text" class="form-control" id="note" placeholder="Enter Assignee Name" name="AssignedTo" />
                    </div>

                    <div class="form-group">
                        <label for="note">Story Points</label>
                        <input type="number" class="form-control" id="note" placeholder="Enter StoryPoints" name="StoryPoints" />
                    </div>

                    <div class="form-group">
                        <label for="date">Due Date</label>
                        <input type="datetime-local" class="form-control" id="date" placeholder="Enter date" required name="DueDate">
                    </div>
                    <p class="text-center">
                        <button type="submit" style="background-color:#4111f0; float:right" class="btn btn-success">Add</button>
                        <button type="button" style="background-color:gray; float:left" class="btn btn-danger" data-dismiss="modal">Cancel</button>
                    </p>
                </form>
            </div>
        </div>

    </div>
</div>

<script>
    function setData(id,task, description, assignedTo, storyPoints, dueDate)
    {
        debugger;
        document.getElementById("ID").value = id;
        document.getElementById("Task").value = task;
        document.getElementById("DescriptionModal").value = description;
        document.getElementById("AssignedTo").value = assignedTo;
        document.getElementById("StoryPoints").value = storyPoints;
        document.getElementById("DueDateModal").value = dueDate;
    }
</script>

<div class="modal fade" id="myModalview" role="dialog">
    <div class="modal-dialog">

        <!-- Modal content-->
        <div class="modal-content">

            <div class="modal-header" style="background: #2cbac4;">
                <h4 class="modal-title">Add Task</h4>
                <button type="button" class="close" data-dismiss="modal">&times;</button>
            </div>
            <div class="modal-body">
                <form class="form" method="post" action="Home/UpdateToDo">
                    <div class="form-group">
                        <label for="note">Task:</label>
                        <input type="hidden" class="form-control" id="ID" placeholder="ID" required name="ID">
                        <input type="text" class="form-control" id="Task" placeholder="Name" required name="Task">
                    </div>
                    <div class="form-group">
                        <lable form="note">Description</lable>
                        <textarea rows="5" cols="60" class="form-control" id="DescriptionModal" placeholder="Enter Work Description Here" required name="Description"></textarea>
                    </div>
                    <div class="form-group">
                        <label for="note">Assigned To</label>
                        <input type="text" class="form-control" id="AssignedTo" placeholder="Enter Assignee Name" required name="AssignedTo" />
                    </div>
                    <div class="form-group">
                        <label for="note">StoryPoints</label>
                        <input type="number" class="form-control" id="StoryPoints" placeholder="Enter StoryPoints" required name="StoryPoints" />
                    </div>
                    <div class="form-group">
                        <label for="note">DueDate:</label>
                        <input type="datetime" class="form-control" id="DueDateModal" placeholder="Enter date" required name="DueDate">
                    </div>
                    <p class="text-center">
                        <button type="submit" style="background-color:#4111f0; float:right" class="btn btn-success">Save</button>
                        <button type="button" style="background-color:gray; float:left" class="btn btn-danger" data-dismiss="modal">Cancel</button>
                    </p>
                </form>
            </div>
        </div>
     </div>
</div>