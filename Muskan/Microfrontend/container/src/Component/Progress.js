import React, { useState } from "react";
import { LinearProgress } from "@mui/material";

const Progress = () => {
  return (
    <div>
      <LinearProgress />
    </div>
  );
};

export default Progress;
